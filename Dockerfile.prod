# Production Dockerfile for PartyKit
FROM node:20-slim

WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application
COPY . .

# Expose Vite dev server and PartyKit server ports
EXPOSE 5173 1999

# Build the frontend and run partykit
CMD ["npm", "run", "dev:built"]
